//var pi = 3.141592653589793238;

fun log(num){
    if(isinstance(num, Number)) return native lib.math.log(num);
    raise NotImplemented();
}

fun log10(num){
    if(isinstance(num, Number)) return native lib.math.log10(num);
    raise NotImplemented();
}

fun log1p(num){
    if(isinstance(num, Number)) return native lib.math.log1p(num);
    raise NotImplemented();
}